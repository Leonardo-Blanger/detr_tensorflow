from setuptools import find_packages, setup

setup(
    name='detr_tensorflow',
    packages=find_packages(include=['detr_tensorflow']),
    version='0.1.0',
    description='DETR Object Detection architecture in Tensorflow',
    author='Leonardo Blanger',
)
